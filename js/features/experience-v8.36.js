(function(){
 const img=c=>c?.thumbnailImageSources?.[0]||c?.thumbnailUrl||c?.image||c?.fullImageUrl||"";
 window.ppcDeckArtFan=d=>{let cs=[];try{cs=deckItems(d).map(x=>x.card).filter(Boolean)}catch(e){}const u=[];for(const c of cs){if(!u.some(x=>x.id===c.id))u.push(c);if(u.length>=3)break}return `<div class="deckArtFan">${u.map(c=>img(c)?`<img loading="lazy" src="${esc(img(c))}" alt="${esc(c.name)}" onerror="this.style.display='none'">`:``).join('')||`<div class="deckArtEmpty"><strong>20-card deck</strong><br><span class="muted tiny">Add cards to reveal deck art</span></div>`}</div>`};
 window.entryScreenV836Legacy=function(){const r=document.getElementById('app');if(!r)return;r.innerHTML=`<div class="foundationEntryShell"><section class="foundationEntryStory"><span class="eyebrow">INDEPENDENT TCG POCKETNEXUS</span><h1>Build smarter.<br>Play prepared.</h1><p>One dark workspace for your 20-card decks, binder, battles, ranked climb, tournament scouting and improvement.</p><div class="entryFeatureGrid"><article><span>PLAY</span><strong>Record battles fast</strong><small>Ranked, casual, sessions and Gym Battle.</small></article><article><span>BUILD</span><strong>Deck + binder workspace</strong><small>Build lists and see what you can finish.</small></article><article><span>COMPETE</span><strong>Scout the field</strong><small>Meta, events and deck-pair preparation.</small></article><article><span>IMPROVE</span><strong>Learn from your games</strong><small>Performance and coaching from your history.</small></article></div><p class="entryIndependent">Independent third-party companion • Not affiliated with The Pokémon Company</p></section><section class="foundationEntryCard"><span class="eyebrow">YOUR POCKET WORKSPACE</span><h2>Continue your game plan</h2><p class="muted">Guest mode is instant and local. Sign in when you want supported cloud sync.</p><button class="oauthBtn googleBtn" data-entry-action="google"><span>G</span> Continue with Google</button><button class="oauthBtn appleBtn" data-entry-action="apple"><span>●</span> Sign in with Apple</button><div class="entryDivider"><span>or</span></div><label>Email<input id="entryEmail" type="email" placeholder="you@example.com" autocomplete="email"></label><label>Password<input id="entryPassword" type="password" placeholder="Password" autocomplete="current-password"></label><button class="entrySignIn" data-entry-action="email-signin">Sign in</button><div class="entryLinkRow"><button class="secondary" data-entry-action="forgot">Forgot password?</button><button class="secondary" data-entry-action="register">Create account</button></div><div class="entryDivider"><span>or</span></div><button class="guestBtn secondary" data-entry-action="guest">Continue as guest</button><p class="entryNote">Guest data stays in this browser unless you explicitly use cloud sync.</p><div id="entryMessage"></div></section></div>`};
 window.dashboard=function(){const s=stats(),rank=state.rank||{points:0},all=completedMatches().sort((a,b)=>b.timestamp-a.timestamp),key=new Date().toDateString(),today=all.filter(m=>new Date(m.timestamp).toDateString()===key),tw=today.filter(m=>m.result==='win').length,tl=today.filter(m=>m.result==='loss').length,d=state.decks.find(x=>x.id===all[0]?.deckId)||state.decks.find(x=>x.id===state.selected)||state.decks[0];document.getElementById('app').innerHTML=`<section class="pocketCommandHero"><div class="pocketCommandCopy"><span class="eyebrow">YOUR COMPETITIVE POCKET</span><h1>What are you doing next?</h1><p>Jump straight into the task in front of you. Your decks, results, meta reads and practice tools stay connected underneath.</p><div class="row"><button onclick="goPage('matches')">Record a Battle</button><button class="secondary" onclick="goPage('decks')">Build a Deck</button><button class="secondary" onclick="goPage('meta')">Scout Meta</button></div></div><aside class="pocketCommandToday"><div><span class="eyebrow">TODAY</span><strong>${tw}-${tl}</strong><p class="muted">${today.length?`${today.length} matches • ${((tw/today.length)*100).toFixed(0)}% WR`:'No matches yet'}</p></div><div class="todayStats"><div><span class="muted tiny">RANK RP</span><b>${rankBorderFmt(rank.points)}</b></div><div><span class="muted tiny">CURRENT DECK</span><b>${esc(d?.name||'None')}</b></div></div></aside></section><section class="pocketHubGrid"><button class="pocketHubCard play" onclick="goPage('matches')"><span class="hubIcon">VS</span><span class="eyebrow">PLAY</span><h2>Battle & Rank</h2><p>Log a match in seconds, continue your climb or run a Gym lineup.</p><span class="hubFoot"><b>${s.w}-${s.l} overall</b><span>Open →</span></span></button><button class="pocketHubCard build" onclick="goPage('decks')"><span class="hubIcon">20</span><span class="eyebrow">BUILD</span><h2>Decks & Collection</h2><p>Assemble 20-card lists and see which builds your binder can complete.</p><span class="hubFoot"><b>${state.decks.length} saved decks</b><span>Open →</span></span></button><button class="pocketHubCard compete" onclick="goPage('meta')"><span class="hubIcon">#1</span><span class="eyebrow">COMPETE</span><h2>Meta & Events</h2><p>Scout what you are likely to face, inspect events, then prepare a pair.</p><span class="hubFoot"><b>Field → Pairing</b><span>Scout →</span></span></button><button class="pocketHubCard improve" onclick="goPage('stats')"><span class="hubIcon">↗</span><span class="eyebrow">IMPROVE</span><h2>Performance</h2><p>Turn your own match history into trends, matchup reads and coaching.</p><span class="hubFoot"><b>${s.t?s.wr.toFixed(0)+'% WR':'Need matches'}</b><span>Review →</span></span></button></section><div class="foundationIntelGrid">${dashboardRankWidget()}${dashboardMetaCommand()}${dashboardCoachingWidget()}</div><section class="panel homeRecent" style="margin-top:12px"><div class="between"><div><span class="eyebrow">RECENT BATTLES</span><h2>Last results</h2></div><button class="secondary" onclick="goPage('matches')">View all</button></div>${recentMatchesHtml(5)}</section>`};
 window.decks=function(){const rows=filteredDecksByCollection(),q=String(state.deckPrefs?.query||'');document.getElementById('app').innerHTML=`<div class="deckManagerPage"><div class="between"><div><span class="eyebrow">BUILD • 20-CARD FORMAT</span><h1>My Decks</h1><p class="muted">Recognize your decks by their cards, not by generic dashboard boxes.</p></div><button onclick="newDeck()">+ New Deck</button></div><section class="panel deckManagerToolbar"><label>Find a deck<input type="search" value="${esc(q)}" placeholder="Search saved decks…" oninput="state.deckPrefs.query=this.value;safeStorageSet(STORE,JSON.stringify(state));decks()"></label><div><span class="eyebrow">SHOW</span><div class="row">${[['all','All'],['favorites','★ Favorites'],['ready','Ready'],['almost','Almost'],['missing','Missing']].map(([k,n])=>`<button class="secondary ${deckBuildFilter===k?'active':''}" onclick="setDeckBuildFilter('${k}')">${n}</button>`).join('')}</div></div><label>Sort<select onchange="setDeckBuildSort(this.value)"><option value="completion" ${deckBuildSort==='completion'?'selected':''}>Completion</option><option value="fewest" ${deckBuildSort==='fewest'?'selected':''}>Fewest missing</option><option value="name" ${deckBuildSort==='name'?'selected':''}>Name</option></select></label></section><div class="deckManagerMeta"><span>${state.decks.length} saved • ${rows.length} shown</span><span>${window.cardLoadMode==='online'?`${CARDS.length.toLocaleString()} cards ready`:'Card library ready'}</span></div><div class="deckManagerGrid">${rows.length?rows.map(({d})=>{const c=deckCollectionStatus(d),x=deckIntelStats(d),fav=deckIsFavorite(d.id);return `<article class="deckManagerCard" onclick="openDeck('${d.id}')">${ppcDeckArtFan(d)}<div class="deckManagerCardBody"><div class="deckManagerCardTop"><div><span class="eyebrow">${isDeckLegal(d)?'READY TO PLAY':'BUILDING'}</span><h3>${esc(d.name)}</h3><p class="muted tiny">${deckCount(d)}/20 cards${d.energy?` • ${esc(d.energy)}`:''}</p></div><button class="deckFavoriteBtn ${fav?'active':''}" onclick="event.stopPropagation();toggleDeckFavorite('${d.id}')">${fav?'★':'☆'}</button></div><div class="deckManagerMetrics"><span><b>${x.record||'—'}</b><small>Record</small></span><span><b>${x.winRate||'—'}</b><small>Win rate</small></span><span><b>${c.pct}%</b><small>Owned</small></span></div><div class="deckManagerFooter"><span class="deckOwned ${c.missing?'needs':'ready'}">${c.collectionKnown?(c.missing?`${c.missing} missing`:'Collection ready'):'Collection not tracked'}</span><div class="row"><button class="secondary" onclick="event.stopPropagation();renameDeckById('${d.id}')">Rename</button><button onclick="event.stopPropagation();openDeck('${d.id}')">Open Deck</button></div></div></div></article>`}).join(''):`<section class="panel deckManagerEmpty"><span class="eyebrow">EMPTY DECK BOX</span><h2>${state.decks.length?'No decks match these filters.':'Build your first 20-card deck'}</h2><p class="muted">Create a deck and it becomes available to Battle, Pairing, Collection and Performance.</p><button onclick="newDeck()">+ New Deck</button></section>`}</div><details class="panel"><summary><strong>Import a 20-card deck list</strong></summary><textarea id="imp" placeholder="2 Card Name&#10;2 Trainer Name"></textarea><button onclick="importDeck()">Import to New Deck</button></details></div>`};
 window.setBattleViewTab=t=>{state.battlePrefs.viewTab=t;save();standardMatches()};
 window.standardMatches=function(){const tab=state.battlePrefs.viewTab||'record',today=completedMatches().filter(m=>new Date(m.timestamp).toDateString()===new Date().toDateString()),ts=wl(today),st=streakInfo(),active=state.sessions.find(s=>!s.end);let body='';if(tab==='record')body=`${battleRankOverviewHtml()}${activeSessionBar(active)}${battleResultSummaryHtml(battleSaveNotice)}<div class="battleRecordGrid"><section class="battleRecordCard">${battleFormHtml()}</section><aside><section class="panel battlePulseCard"><span class="eyebrow">TODAY</span><h2>Session pulse</h2><div class="metricgrid"><div class="metric"><div class="l">Record</div><div class="n">${ts.w}-${ts.l}</div></div><div class="metric"><div class="l">Win rate</div><div class="n">${ts.n?ts.wr.toFixed(0)+'%':'—'}</div></div><div class="metric"><div class="l">Streak</div><div class="n">${st.type==='none'?'—':(st.type==='win'?'W':'L')+st.count}</div></div></div></section><section class="panel battleRecentCard" style="margin-top:12px"><div class="between"><h2>Recent battles</h2><button class="secondary" onclick="setBattleViewTab('history')">History</button></div>${recentMatchesHtml(5)}</section></aside></div>`;if(tab==='sessions')body=`<section class="grid"><div class="panel"><div class="between"><div><span class="eyebrow">BATTLE SESSIONS</span><h2>Focused runs</h2></div>${active?'':sessionStartControls()}</div>${sessionHistoryHtml()}</div><aside class="panel"><span class="eyebrow">CONTEXT</span><h2>Separate testing from ladder</h2><p class="muted">Use sessions for ranked grinds, tournament practice or deck testing so later performance reads keep the right context.</p></aside></section>`;if(tab==='history')body=`<section class="panel"><div class="between"><div><span class="eyebrow">BATTLE HISTORY</span><h2>Your recorded games</h2></div><div class="row"><button class="secondary" onclick="exportBattleJSON()">JSON</button><button class="secondary" onclick="exportBattleCSV()">CSV</button></div></div>${battleFiltersHtml()}<div id="battleHistory"></div></section>`;document.getElementById('app').innerHTML=`<div class="between"><div><span class="eyebrow">PLAY • BATTLE TRACKER</span><h1>Battle</h1><p class="muted">Record first. Sessions and history stay one tap away.</p></div><div class="row"><button class="secondary" onclick="goPage('rank')">Rank</button><button class="secondary" onclick="goPage('stats')">Performance</button></div></div><nav class="battleTabs">${[['record','Record'],['sessions','Sessions'],['history','History']].map(([k,n])=>`<button class="${tab===k?'active':''}" onclick="setBattleViewTab('${k}')">${n}</button>`).join('')}</nav>${body}`;if(tab==='history')renderBattleHistory()};
 window.gymSetupPlayerCard=function(side,p,i){const home=side==='home',a=home?gymDeckName(p.deck1)||'Choose Deck A':p.deck1||'Unknown Deck A',b=home?gymDeckName(p.deck2)||'Choose Deck B':p.deck2||'Unknown Deck B';return `<details class="gymRosterPlayer" ${i===0?'open':''}><summary><span class="gymPlayerNum">${i+1}</span><span><strong>${esc(p.name||`Player ${i+1}`)}</strong><small>${esc(a)} + ${esc(b)}</small></span><span class="pill">2 decks</span></summary><div class="gymPlayerFields"><label>Player name<input value="${esc(p.name)}" onchange="gymUpdateSetup('${side}',${i},'name',this.value)"></label>${home?`<div class="gymDeckPickRow"><label>Deck A<select onchange="gymUpdateSetup('home',${i},'deck1',this.value)">${gymSetupDeckOptions(p.deck1)}</select></label><label>Deck B<select onchange="gymUpdateSetup('home',${i},'deck2',this.value)">${gymSetupDeckOptions(p.deck2)}</select></label></div><button class="secondary" onclick="gymOpenPairing(${i})">Find Best Pair</button>`:`<div class="gymDeckPickRow"><label>Deck A<input value="${esc(p.deck1)}" placeholder="Archetype" onchange="gymUpdateSetup('away',${i},'deck1',this.value)"></label><label>Deck B<input value="${esc(p.deck2)}" placeholder="Archetype" onchange="gymUpdateSetup('away',${i},'deck2',this.value)"></label></div>`}</div></details>`};
 window.gymSetupPage=function(){const g=gymState(),top=gymAllPairs()[0];return `<div class="between"><div><span class="eyebrow">PLAY • TEAM FORMAT</span><h1>Gym Battle</h1><p class="muted">Five players. Two decks each. Winner stays; the losing deck is eliminated.</p></div><button class="secondary" onclick="gymSetView('history')">History</button></div><section class="panel gymRules"><div><strong>5</strong><span>Players / Gym</span></div><div><strong>2</strong><span>Decks / Player</span></div><div><strong class="good">WIN</strong><span>Keep deck</span></div><div><strong class="bad">LOSS</strong><span>Lose deck</span></div></section>${top?`<section class="panel between"><div><span class="eyebrow">PAIRING LAB</span><h2>${esc(top.a.name)} + ${esc(top.b.name)}</h2><p class="muted">Current best pair • Score ${top.score}/100</p></div><button onclick="gymOpenPairing(0)">Prepare Pair →</button></section>`:''}<div class="gymNames"><label>Your Gym<input value="${esc(g.homeGym)}" onchange="gymState().homeGym=this.value;save()"></label><label>Opponent Gym<input value="${esc(g.awayGym)}" onchange="gymState().awayGym=this.value;save()"></label></div><div class="gymRosterGrid"><section class="panel"><div class="between"><div><span class="eyebrow">YOUR TEAM</span><h2>5-player lineup</h2></div><span class="pill">10 deck slots</span></div><div class="gymRosterList">${g.homePlayers.map((p,i)=>gymSetupPlayerCard('home',p,i)).join('')}</div></section><section class="panel"><div class="between"><div><span class="eyebrow">SCOUTED TEAM</span><h2>Opponent lineup</h2></div><span class="pill">Manual scouting</span></div><div class="gymRosterList">${g.awayPlayers.map((p,i)=>gymSetupPlayerCard('away',p,i)).join('')}</div></section></div><section class="panel between"><div><strong>Lineups ready?</strong><p class="muted tiny">Results save locally after every game.</p></div><button onclick="gymStartBattle()">Start Gym Battle</button></section>`};
 const oldCollection=window.collectionPage;window.collectionPage=function(){oldCollection();const r=document.getElementById('app'),h=r?.querySelector(':scope > .between > div');if(h&&!h.querySelector('.eyebrow'))h.insertAdjacentHTML('afterbegin','<span class="eyebrow">BUILD • DIGITAL BINDER</span>')};
 const oldMeta=window.metaPage;window.metaPage=function(){oldMeta();document.getElementById('app')?.classList.add('competitiveMetaPage')};
 window.morePage=function(){document.getElementById('app').innerHTML=`<div><span class="eyebrow">UTILITY DRAWER</span><h1>More</h1><p class="muted">Secondary tools stay here so the four main player workflows remain focused.</p></div><section class="toolLaunchGrid"><button class="toolLaunchCard" onclick="goPage('trade')"><span>⇄</span><strong>Trade</strong><small>Have ↔ Want binder</small></button><button class="toolLaunchCard" onclick="goPage('streamer')"><span>◉</span><strong>Streamer</strong><small>Session + overlay control</small></button><button class="toolLaunchCard" onclick="openGlobalSearch()"><span>⌕</span><strong>Search</strong><small>Pages, cards and decks</small></button><button class="toolLaunchCard" onclick="window.PPCWhatsNew?.open?.(true)"><span>✦</span><strong>What's New</strong><small>Player-facing updates</small></button><button class="toolLaunchCard" onclick="goPage('account')"><span>●</span><strong>Account & Cloud</strong><small>Sign-in, backup and sync</small></button><button class="toolLaunchCard" onclick="goPage('about')"><span>i</span><strong>About & Privacy</strong><small>Data and third-party notice</small></button></section><details class="panel" style="margin-top:14px"><summary><strong>Advanced & troubleshooting</strong></summary><p class="muted">Diagnostics and raw exports stay out of the everyday player workflow.</p><div class="row"><button class="secondary" onclick="runMetaDiagnostics()">Meta Diagnostics</button><button class="secondary" onclick="runFullDiagnostics()">Full Diagnostics</button><button class="secondary" onclick="runPrePublicEnvironmentTests()">Environment Tests</button></div></details>`};
 try{state.dataVersion=Math.max(Number(state.dataVersion||0),8.36);save()}catch(e){}
})();

/* V8.38 Player Experience Polish — targeted page/component refinements */
(function(){
 const tradeCard=(c,kind)=>{const rec=collectionRead(c.id),qty=kind==='have'?rec.tradeable:rec.wanted;return `<button class="tradeCard" onclick="openCardModal('${c.id}')"><div class="tradeCardArt">${imageTag(c,'thumb')}</div><div class="tradeCardBody"><strong>${esc(c.name)}</strong><small>${esc(c.setCode||c.setName||'')} ${c.rarity?`• ${esc(c.rarity)}`:''}</small><span>${kind==='have'?'Tradeable':'Wanted'} ×${qty}</span></div></button>`};
 window.tradePage=function(){
  const have=CARDS.filter(c=>collectionRead(c.id).tradeable>0),want=CARDS.filter(c=>collectionRead(c.id).wanted>0);
  const app=document.getElementById('app'); if(!app)return;
  app.innerHTML=`<div class="between"><div><span class="eyebrow">MORE • TRADE</span><h1>Trade Center</h1><p class="muted">Your Collection powers a simple Have ↔ Want binder.</p></div><button class="secondary" onclick="goPage('collection')">Manage Collection</button></div>
  <section class="tradeSummary"><article><span>I HAVE</span><strong>${have.length}</strong><small>cards marked tradeable</small></article><div class="tradeSwap">⇄</div><article><span>I WANT</span><strong>${want.length}</strong><small>cards on your wishlist</small></article></section>
  <div class="tradeColumns"><section class="panel"><div class="between"><div><span class="eyebrow">TRADE BINDER</span><h2>I Have</h2></div><span class="pill">${have.length}</span></div>${have.length?`<div class="tradeCardGrid">${have.map(c=>tradeCard(c,'have')).join('')}</div>`:`<div class="tradeEmpty"><span>⇄</span><h3>No tradeable cards yet</h3><p>Mark a card as Trade in Collection and it will appear here.</p><button onclick="goPage('collection')">Open Collection</button></div>`}</section>
  <section class="panel"><div class="between"><div><span class="eyebrow">WISHLIST</span><h2>I Want</h2></div><span class="pill">${want.length}</span></div>${want.length?`<div class="tradeCardGrid">${want.map(c=>tradeCard(c,'want')).join('')}</div>`:`<div class="tradeEmpty"><span>◇</span><h3>Your wishlist is empty</h3><p>Mark cards as Want in Collection to build your trade targets.</p><button onclick="goPage('collection')">Browse Collection</button></div>`}</section></div>
  <details class="panel tradeDetails"><summary><strong>How Trade Center works</strong></summary><p class="muted">Owned, wanted, and tradeable quantities stay controlled from Collection so there is one source of truth.</p></details>`;
 };

 const originalStreamerPage=window.streamerPage;
 // V8.46.1: keep the Streamer Overlay 3.0 controller defined in streamer_rank_tools.js.
 // The V8.36 experience layer previously replaced streamerPage here, which hid all V8.46 controls.
 if(typeof originalStreamerPage === "function") window.streamerPage = originalStreamerPage;

 window.aboutPage=function(){
  document.getElementById('app').innerHTML=`<div class="between"><div><span class="eyebrow">ABOUT</span><h1>PocketNexus</h1><p class="muted">Independent tools for Pokémon TCG Pocket players.</p></div><button class="secondary" onclick="goPage('account')">Account & Data</button></div><div class="aboutGrid"><section class="panel"><span class="eyebrow">INDEPENDENT & UNOFFICIAL</span><h2>Built for players, not by Pokémon</h2><p>PocketNexus is an independent third-party companion. It is not an official Pokémon product, tournament platform, Limitless application, or service affiliated with The Pokémon Company.</p><p class="muted">Pokémon names, game terminology, and related marks belong to their respective owners.</p></section><section class="panel"><span class="eyebrow">YOUR DATA</span><h2>Local first, cloud when you choose</h2><p>Guest mode keeps decks, Collection, matches, rank information, sessions, and preferences in this browser.</p><p class="muted">When you sign in and use cloud sync, selected app data can be copied to your account. Restore creates a local safety backup first.</p></section><section class="panel"><span class="eyebrow">COMPETITIVE DATA</span><h2>Useful context, not official standings</h2><p>Meta and tournament information may come from third-party or community sources. Rank-border forecasts are independent estimates.</p><p class="muted">Always use Pokémon TCG Pocket itself for your official RP, rank, season status, and match results.</p></section><section class="panel"><span class="eyebrow">ACCOUNT & PRIVACY</span><h2>Control your sync</h2><p>Signing in does not mean every local record must be uploaded. Account & Cloud gives you controls for sync, backup, and restore.</p><button class="secondary" onclick="goPage('account')">Review Account & Cloud</button></section></div>`;
 };

 window.morePage=function(){
  document.getElementById('app').innerHTML=`<div><span class="eyebrow">UTILITY DRAWER</span><h1>More</h1><p class="muted">Secondary tools stay here so Play, Build, Compete, and Improve remain focused.</p></div><section class="toolLaunchGrid"><button class="toolLaunchCard" onclick="goPage('trade')"><span>⇄</span><strong>Trade</strong><small>Have ↔ Want binder</small></button><button class="toolLaunchCard" onclick="goPage('streamer')"><span>◉</span><strong>Streamer</strong><small>Session + overlay control</small></button><button class="toolLaunchCard" onclick="openGlobalSearch()"><span>⌕</span><strong>Search</strong><small>Pages, cards and decks</small></button><button class="toolLaunchCard" onclick="window.PPCWhatsNew?.open?.(true)"><span>✦</span><strong>What's New</strong><small>Player-facing updates</small></button><button class="toolLaunchCard" onclick="goPage('account')"><span>●</span><strong>Account & Cloud</strong><small>Sign-in, backup and sync</small></button><button class="toolLaunchCard" onclick="goPage('about')"><span>i</span><strong>About & Privacy</strong><small>Data and third-party notice</small></button><button class="toolLaunchCard" onclick="document.getElementById('settingsSection')?.scrollIntoView({behavior:'smooth'})"><span>⚙</span><strong>Settings</strong><small>Appearance and local data</small></button></section><section class="moreSettingsGrid" id="settingsSection"><article class="panel"><span class="eyebrow">APPEARANCE</span><h2>Theme</h2><p class="muted">Use the Dark control in the header to switch appearance.</p></article><article class="panel"><span class="eyebrow">DATA SAFETY</span><h2>Local backup</h2><p class="muted">Keep a portable copy of your local PocketNexus data.</p><div class="row"><button onclick="backupAllData()">Download Backup</button><button class="secondary" onclick="restoreBackupPrompt()">Restore</button></div></article></section><details class="panel" style="margin-top:14px"><summary><strong>Advanced & troubleshooting</strong></summary><p class="muted">Diagnostics and raw exports stay out of the everyday player workflow.</p><div class="row"><button class="secondary" onclick="runMetaDiagnostics()">Meta Diagnostics</button><button class="secondary" onclick="runFullDiagnostics()">Full Diagnostics</button><button class="secondary" onclick="runPrePublicEnvironmentTests()">Environment Tests</button></div></details>`;
 };

 try{state.dataVersion=Math.max(Number(state.dataVersion||0),8.39);save()}catch(e){}
})();
