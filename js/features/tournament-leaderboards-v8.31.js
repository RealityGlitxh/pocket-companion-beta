(function(){
"use strict";
const escLocal=v=>typeof esc==='function'?esc(v):String(v??"").replace(/[&<>\"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;"}[c]));
const fmtDate=v=>{if(!v)return"Date unavailable";const d=new Date(v);return Number.isFinite(d.getTime())?d.toLocaleDateString(undefined,{month:"short",day:"numeric",year:"numeric"}):String(v)};
function prefs(){state.tournamentPrefs=state.tournamentPrefs&&typeof state.tournamentPrefs==="object"?state.tournamentPrefs:{};const p=state.tournamentPrefs;p.selectedKey=String(p.selectedKey||"");p.query=String(p.query||"");p.view=["leaderboard","decks","events"].includes(p.view)?p.view:"leaderboard";p.minPlayers=Math.max(0,Number(p.minPlayers||0));p.format=String(p.format||"");p.limit=Math.min(250,Math.max(50,Number(p.limit||150)));return p}
function fallbackEvents(){return (typeof MetaSnapshotService!=="undefined"?MetaSnapshotService.getTournaments():[]).map((t,i)=>({id:`fallback-${i}`,external_id:"",name:t.name,date:t.date,start_date:t.date,player_count:Number(t.players)||0,format:"Snapshot",tracked_players:(t.finishers||[]).length,source:"Bundled snapshot",finishers:t.finishers||[],fallback:true}))}
function liveEvents(){const rows=window.PPCTournamentService?.getCatalog?.()||[];return rows.map(t=>({...t,date:t.start_date,players:t.player_count,source:"Live catalog",finishers:[]}))}
function events(){const live=liveEvents();return live.length?live:fallbackEvents()}
function selectedEvent(list){if(!list.length)return null;const p=prefs();return list.find(t=>String(t.id)===p.selectedKey)||list[0]}
function recordText(r){return`${Number(r.wins)||0}-${Number(r.losses)||0}-${Number(r.ties)||0}`}
function deckCountsFromRows(rows){const m=new Map();rows.forEach(r=>{const raw=r.archetype_name||"Unknown";const k=raw==="Unknown"?raw:(typeof canonicalArchetypeName==="function"?canonicalArchetypeName(raw):raw);const x=m.get(k)||{deck:k,count:0,best:999,players:[]};x.count++;x.best=Math.min(x.best,Number(r.placement)||999);x.players.push(r.display_name);m.set(k,x)});return[...m.values()].sort((a,b)=>b.count-a.count||a.best-b.best||a.deck.localeCompare(b.deck))}
async function ensureData(force=false){const p=prefs();if(!window.PPCTournamentService)return tournamentsPage();try{await window.PPCTournamentService.loadCatalog({minPlayers:p.minPlayers,format:p.format,search:p.query,limit:p.limit},{force});const list=events();const t=selectedEvent(list);if(t&&!t.fallback){try{const rows=await window.PPCTournamentService.loadStandings(t.id,{force});window.__ppcTournamentRows=window.__ppcTournamentRows||{};window.__ppcTournamentRows[t.id]=Array.isArray(rows)?rows:[];window.__ppcTournamentStandingsAttempted=window.__ppcTournamentStandingsAttempted||{};window.__ppcTournamentStandingsAttempted[t.id]=Date.now()}catch(e){console.warn('Tournament standings refresh failed',e)}}}catch(e){console.warn('Tournament refresh failed',e)}tournamentsPage()}
function setSelected(id){prefs().selectedKey=String(id||"");save();ensureSelectedStandings()}
async function ensureSelectedStandings(){const t=selectedEvent(events());if(t&&!t.fallback&&window.PPCTournamentService){try{await window.PPCTournamentService.loadStandings(t.id)}catch(e){console.warn(e)}}tournamentsPage()}
function setView(v){prefs().view=v;save();tournamentsPage()}
function setQuery(v){prefs().query=String(v||"");save();clearTimeout(window.__ppcTournamentSearchTimer);window.__ppcTournamentSearchTimer=setTimeout(()=>ensureData(true),250)}
function setFilter(kind,v){const p=prefs();if(kind==="players")p.minPlayers=Math.max(0,Number(v||0));if(kind==="format")p.format=String(v||"");p.selectedKey="";save();ensureData(true)}
function currentRows(t){if(!t)return[];if(t.fallback)return(t.finishers||[]).map(f=>({placement:f.place,display_name:f.player,source_player_id:"",country:"",wins:0,losses:0,ties:0,archetype_name:f.deck||"Unknown",deck_url:f.listUrl||""}));const cache=window.PPCTournamentService?.status?.();const entry=window.PPCTournamentService?.loadStandings?null:null;return window.__ppcTournamentRows?.[t.id]||[]}
async function rowsFor(t){if(!t)return[];if(t.fallback)return currentRows(t);const rows=await window.PPCTournamentService.loadStandings(t.id);window.__ppcTournamentRows=window.__ppcTournamentRows||{};window.__ppcTournamentRows[t.id]=rows;return rows}
function getCachedRows(t){if(!t)return[];if(t.fallback)return currentRows(t);return window.__ppcTournamentRows?.[t.id]||[]}
async function refreshSelected(force=false){const t=selectedEvent(events());if(t&&!t.fallback){try{const rows=await window.PPCTournamentService.loadStandings(t.id,{force});window.__ppcTournamentRows=window.__ppcTournamentRows||{};window.__ppcTournamentRows[t.id]=rows}catch(e){console.warn(e)}}tournamentsPage()}
function useForPairing(){const t=selectedEvent(events());if(!t)return;const rows=getCachedRows(t),targets=deckCountsFromRows(rows).filter(x=>x.deck!=="Unknown").slice(0,4).map(x=>typeof canonicalArchetypeName==="function"?canonicalArchetypeName(x.deck):x.deck);const g=typeof gymState==='function'?gymState():(state.gymBattle=state.gymBattle||{});g.scoutTargets=targets;g.pairingMode="scout";g.pairingPlayer=Number(g.pairingPlayer||0);g.view=state.decks?.length>=2?"pairing":"setup";state.battlePrefs=state.battlePrefs||{};state.battlePrefs.experienceMode="gym";save();if(typeof goPage==='function')goPage('matches')}
async function openPlayer(id){if(!id||!window.PPCTournamentService)return;const root=document.getElementById("tournamentPlayerProfile");if(root)root.innerHTML='<div class="tournamentEmpty">Loading player history…</div>';try{const p=await window.PPCTournamentService.loadPlayer(id,{force:true});if(!root)return;if(!p){root.innerHTML='<div class="tournamentEmpty">No tracked history yet.</div>';return}const games=Number(p.wins||0)+Number(p.losses||0)+Number(p.ties||0);root.innerHTML=`<article class="tournamentPlayerCard"><div><span class="eyebrow">TRACKED PLAYER</span><h3>${escLocal(p.display_name||p.source_player_id)}</h3><small>${escLocal(p.country||"Country unavailable")} • Player ID ${escLocal(p.source_player_id)}</small></div><div class="tournamentPlayerMetrics"><b>${Number(p.tournaments)||0}<span>events</span></b><b>${games}<span>games</span></b><b>#${Number(p.best_finish)||"—"}<span>best finish</span></b><b>${p.average_finish??"—"}<span>avg finish</span></b></div><p class="muted tiny">This is the tracking foundation for a future tournament player ranking. No ranking formula is being invented yet.</p></article>`}catch(e){if(root)root.innerHTML=`<div class="tournamentEmpty">Could not load player history: ${escLocal(e?.message||e)}</div>`}}
function leaderboardView(t,rows){const q=String(prefs().query||"").trim().toLowerCase();const shown=rows.filter(r=>!q||`${r.display_name} ${r.archetype_name} ${r.placement}`.toLowerCase().includes(q));const podium=shown.filter(r=>Number(r.placement)<=3).slice(0,3);return `${podium.length?`<div class="tournamentPodium">${podium.map(r=>`<article class="podiumCard place${r.placement}"><span class="podiumPlace">#${r.placement}</span><strong>${escLocal(r.display_name)}</strong><small>${escLocal(r.archetype_name||"Deck unavailable")}</small><b>${t.fallback?'Record unavailable':recordText(r)}</b></article>`).join("")}</div>`:""}<section class="panel tournamentLeaderboardPanel"><div class="between tournamentTableHead"><div><span class="eyebrow">COMPLETE STANDINGS</span><h2>${t.fallback?'Tracked snapshot':'Tournament standings'}</h2><p class="muted">${t.fallback?'Cached snapshot is active.':'Every available standing is stored independently from deck availability, so event coverage remains useful even when some lists are missing.'}</p></div></div><div class="tournamentColumnLabels"><span>Place</span><span>Player</span><span>Deck</span><span>Record</span><span>Actions</span></div><div id="tournamentLeaderboardRows">${shown.length?shown.map((r,i)=>{const place=Number(r.placement)||i+1;return`<div class="tournamentRow ${place===1?'gold':place===2?'silver':place===3?'bronze':''}"><div class="tournamentPlace"><strong>${place}</strong></div><div class="tournamentPlayer"><strong>${escLocal(r.display_name||"Unknown player")}</strong><small>${escLocal(r.country||r.source_player_id||"Tracked player")}</small></div><div class="tournamentDeck"><strong>${escLocal(r.archetype_name||"Unknown / no public deck")}</strong><small>${r.deck_url?'Public deck list':'No public deck list'}</small></div><div class="tournamentRecord"><strong>${t.fallback?'—':recordText(r)}</strong><small>W-L-T</small></div><div class="tournamentDeckAction"><div class="row">${r.source_player_id?`<button class="secondary" onclick="tournamentOpenPlayer('${escLocal(r.source_player_id)}')">Player</button>`:''}${r.deck_url?`<a class="secondary buttonLike" href="${escLocal(r.deck_url)}" target="_blank" rel="noopener noreferrer">Deck ↗</a>`:''}</div></div></div>`}).join(""):'<div class="tournamentEmpty tournamentEmptyRecovery"><strong>Standings are not available for this event yet.</strong><span>Try another event or refresh this one. The rest of the tournament catalog remains usable.</span><button class="secondary" onclick="tournamentRefresh()">Retry standings</button></div>'}</div></section><div id="tournamentPlayerProfile"></div>`}
function decksView(rows){const decks=deckCountsFromRows(rows);return`<section class="panel"><div class="between"><div><span class="eyebrow">EVENT DECKS</span><h2>Represented archetypes</h2><p class="muted">Counts use all tracked standings with a classified public deck list.</p></div><button onclick="tournamentUseForPairing()">Scout These Decks</button></div><div class="tournamentDeckStats">${decks.map((d,i)=>`<article><span class="metaRank">${i+1}</span><div><strong>${escLocal(d.deck)}</strong><small>${d.count} player${d.count===1?'':'s'} • best finish #${d.best===999?'—':d.best}</small></div><b>${d.count}</b></article>`).join("")||'<p class="muted">No classified decks are available for this event yet.</p>'}</div></section>`}
function eventsView(list){return`<section class="panel"><div class="between"><div><span class="eyebrow">EVENT BROWSER</span><h2>Pokémon Pocket tournament catalog</h2><p class="muted">Browse the stored competitive event catalog with participant, format, and search filters.</p></div></div><div class="tournamentEventGrid">${list.map(t=>`<button class="tournamentEventCard" onclick="tournamentSelect('${escLocal(t.id)}')"><span>${fmtDate(t.start_date||t.date)} • ${escLocal(t.format||'Unknown format')}</span><strong>${escLocal(t.name)}</strong><small>${Number(t.player_count??t.players)||0} participants • ${Number(t.tracked_players)||(t.finishers||[]).length} tracked standings</small><i>${t.import_status==='complete'?'Standings ready':'Queued / importing'} →</i></button>`).join("")||'<div class="tournamentEmpty">No tournaments match these filters.</div>'}</div></section>`}
function tournamentsPage(){try{const root=document.getElementById("app");if(!root)return;const p=prefs(),svc=window.PPCTournamentService,status=svc?.status?.()||{},list=events();if(!list.length){if(status.error&&!status.loading){root.innerHTML=`<section class="tournamentHero"><div><span class="eyebrow">LIVE LIMITLESS TOURNAMENTS</span><h1>Tournament Intelligence</h1><p>The tournament catalog could not be loaded.</p></div></section><section class="panel"><div class="dangerBox"><strong>Catalog unavailable</strong><p>${escLocal(status.error)}</p></div><div class="row"><button onclick="tournamentRefresh()">Retry</button><button class="secondary" onclick="headerNavigate('dashboard')">Back Home</button></div><p class="muted">Your other PocketNexus data is unaffected.</p></section>`;return}root.innerHTML=`<section class="tournamentHero"><div><span class="eyebrow">LIVE LIMITLESS TOURNAMENTS</span><h1>Tournament Intelligence</h1><p>Loading the Pokémon TCG Pocket event catalog…</p></div></section><section class="panel"><div class="spinner"></div><p class="muted">The collector discovers tournaments in the background and imports complete standings separately.</p></section>`;if(!status.loading)ensureData(false);return}const t=selectedEvent(list);if(!p.selectedKey&&t){p.selectedKey=String(t.id);save()}const rows=getCachedRows(t);if(t&&!t.fallback&&!rows.length&&!window.__ppcTournamentLoading?.[t.id]&&!window.__ppcTournamentStandingsAttempted?.[t.id]){window.__ppcTournamentLoading=window.__ppcTournamentLoading||{};window.__ppcTournamentStandingsAttempted=window.__ppcTournamentStandingsAttempted||{};window.__ppcTournamentLoading[t.id]=true;window.__ppcTournamentStandingsAttempted[t.id]=Date.now();rowsFor(t).then(()=>{window.__ppcTournamentLoading[t.id]=false;tournamentsPage()}).catch(e=>{window.__ppcTournamentLoading[t.id]=false;console.warn('Tournament standings auto-load failed',e);tournamentsPage()})}const sync=svc?.getSyncStatus?.()||{},formats=svc?.getFormats?.()||[];const dcounts=deckCountsFromRows(rows),coverage=Number(t.player_count)>0?Math.min(100,Number(t.tracked_players||rows.length)/Number(t.player_count)*100):0;const view=p.view;
root.innerHTML=`<section class="tournamentHero"><div><div class="tournamentHeroKicker"><span class="pocketBallMark"><i></i></span><span>LIVE TOURNAMENT CATALOG</span><b>AUTO REFRESH</b></div><h1>Tournament Intelligence</h1><p>Browse Pokémon TCG Pocket events, filter by participants and format, inspect complete standings, and build the player-history dataset for future tournament rankings.</p><div class="row"><button onclick="tournamentRefresh()">Refresh View</button><button class="secondary" onclick="tournamentUseForPairing()">Use Event for Pairing Lab</button></div></div><div class="tournamentHeroCards"><article><span>DISCOVERED</span><strong>${Number(sync.discovered_tournaments)||list.length}</strong><small>tournaments stored</small></article><article><span>PLAYERS</span><strong>${Number(sync.unique_players)||'—'}</strong><small>unique tracked IDs</small></article><article><span>RESULTS</span><strong>${Number(sync.tracked_player_results)||'—'}</strong><small>player-event rows</small></article></div></section>
<section class="panel tournamentFilters"><label>Participants<select onchange="tournamentSetFilter('players',this.value)">${[[0,'Any size'],[8,'8+'],[16,'16+'],[32,'32+'],[64,'64+'],[128,'128+']].map(x=>`<option value="${x[0]}" ${p.minPlayers===x[0]?'selected':''}>${x[1]}</option>`).join('')}</select></label><label>Format<select onchange="tournamentSetFilter('format',this.value)"><option value="">All formats</option>${formats.map(f=>`<option value="${escLocal(f.format)}" ${p.format===f.format?'selected':''}>${escLocal(f.format)} (${f.event_count})</option>`).join('')}</select></label><label class="tournamentFilterSearch">Search<input value="${escLocal(p.query)}" placeholder="Tournament or organizer…" oninput="tournamentSetQuery(this.value)"></label><div class="tournamentSyncBadge"><span class="${status.source==='error'?'bad':status.source==='cached'?'warn':'ok'}">${status.loading&&status.source!=='live'?'REFRESHING':status.source==='cached'?'CACHED':status.source==='error'?'FALLBACK':'LIVE CATALOG'}</span><small>${status.loading?'Refreshing live data…':sync.discovery_last_success?`Last synced ${new Date(sync.discovery_last_success).toLocaleTimeString([], {hour:'numeric',minute:'2-digit'})}`:status.lastSuccess?`Last synced ${new Date(status.lastSuccess).toLocaleTimeString([], {hour:'numeric',minute:'2-digit'})}`:status.error?'Live sync unavailable':'Waiting for first sync'}</small></div></section>
<section class="panel tournamentEventHeader"><div class="tournamentEventIdentity"><span class="eyebrow">SELECTED EVENT</span><h2>${escLocal(t.name)}</h2><p>${fmtDate(t.start_date||t.date)} • ${Number(t.player_count??t.players)||0} participants • ${escLocal(t.format||'Unknown format')} ${t.organizer_name?`• ${escLocal(t.organizer_name)}`:''}</p></div><select class="tournamentEventSelect" onchange="tournamentSelect(this.value)">${list.map(e=>`<option value="${escLocal(e.id)}" ${String(e.id)===String(t.id)?'selected':''}>${fmtDate(e.start_date||e.date)} — ${escLocal(e.name)}</option>`).join('')}</select></section>
<div class="tournamentKpis"><article><span>Tracked standings</span><strong>${Number(t.tracked_players)||rows.length}</strong><small>${coverage.toFixed(1)}% of participants</small></article><article><span>Public deck lists</span><strong>${rows.filter(r=>r.deck_url).length}</strong><small>available in loaded standings</small></article><article><span>Archetypes</span><strong>${dcounts.filter(d=>d.deck!=='Unknown').length}</strong><small>classified in this event</small></article><article><span>Import status</span><strong class="tournamentDeckName">${escLocal(t.fallback?'Snapshot':t.import_status||'discovered')}</strong><small>${t.standings_imported_at?`updated ${new Date(t.standings_imported_at).toLocaleString()}`:'background queue'}</small></article></div>
<div class="notice tournamentDataNotice"><strong>Coverage:</strong> event details, standings, and available deck information are stored separately so the page can stay useful even when one part of an event is incomplete. Cached results remain available during temporary sync problems.</div>
<div class="tournamentTabs"><button class="secondary ${view==='leaderboard'?'active':''}" onclick="tournamentSetView('leaderboard')">Standings</button><button class="secondary ${view==='decks'?'active':''}" onclick="tournamentSetView('decks')">Decks</button><button class="secondary ${view==='events'?'active':''}" onclick="tournamentSetView('events')">All Events</button></div><div id="tournamentViewRoot">${view==='decks'?decksView(rows):view==='events'?eventsView(list):leaderboardView(t,rows)}</div>`;
if(status.error)console.warn("Tournament live service fallback:",status.error)}catch(e){console.error("Tournament page render failed",e);const root=document.getElementById("app");if(root)root.innerHTML=`<section class="tournamentHero"><div><span class="eyebrow">TOURNAMENT INTELLIGENCE</span><h1>We hit a display error.</h1><p>Your tournament data is still safe. Reload this view or return home while PocketNexus retries cleanly.</p></div></section><section class="panel"><div class="dangerBox"><strong>Tournament view recovered</strong><p>${escLocal(String(e?.message||"Unexpected display error").slice(0,220))}</p></div><div class="row"><button onclick="tournamentRefresh()">Retry Tournament View</button><button class="secondary" onclick="headerNavigate('dashboard')">Back Home</button></div></section>`}}
window.tournamentsPage=tournamentsPage;window.tournamentSelect=id=>setSelected(id);window.tournamentSetView=setView;window.tournamentSetQuery=setQuery;window.tournamentSetFilter=setFilter;window.tournamentUseForPairing=useForPairing;window.tournamentOpenPlayer=openPlayer;window.tournamentRefresh=()=>ensureData(true);
})();
